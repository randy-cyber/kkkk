# TripRadar environment variables
# Copy this file to `.env.local` and fill in your Supabase project credentials.
# The UI runs fully on mock data without these — they're only needed once the
# backend is wired up.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
